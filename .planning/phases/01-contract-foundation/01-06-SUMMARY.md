---
phase: 01-contract-foundation
plan: "06"
subsystem: deployment-infrastructure
tags: [hardhat, deploy, etherscan, verification, abi, sepolia, typescript, contracts-config]

# Dependency graph
requires:
  - phase: 01-01
    provides: "NFTContract with ERC-2981 royalty, 2-arg mintNFT"
  - phase: 01-02
    provides: "AuctionHouse English auction with escrow and pull-payment bidding"
  - phase: 01-03
    provides: "AuctionHouse Dutch auction with linear price decay"
  - phase: 01-04
    provides: "CollectionFactory + Collection clone with EIP-1167 minimal proxies"
  - phase: 01-05
    provides: "Marketplace offer system with ETH escrow, auto-unlist on accept"
provides:
  - "Unified deploy.js deploying all 4 contracts in correct order"
  - "deployments/{network}.json canonical address registry (nftContract, marketplace, auctionHouse, collectionFactory, collectionImplementation)"
  - "V1 NFTContract address preservation on redeploy (nftContractV1 field)"
  - "Etherscan verification commands printed after Sepolia deployment"
  - "contracts.ts with complete human-readable ABIs for all 5 contract types"
  - "Phase 1 Contract Foundation complete"
affects:
  - "Phase 2 subgraph (reads deployments/sepolia.json for contract addresses)"
  - "Phase 3 frontend (uses updated CONTRACTS config for all Web3 calls)"
  - "Phase 4 backend (reads contract ABIs from contracts.ts or deployments JSON)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Deployment registry pattern: deploy.js writes deployments/{network}.json; frontend reads via VITE_ env vars"
    - "V1 preservation: redeploy detects prior address, stores it as nftContractV1 for Dashboard dual-address support"
    - "Etherscan verification commands: printed at end of Sepolia deploy for easy copy-paste"
    - "Collection COLLECTION key has no fixed address — used with clone addresses returned by factory"

key-files:
  created:
    - contracts/deployments/.gitkeep
    - contracts/deployments/hardhat.json
    - .planning/phases/01-contract-foundation/01-06-SUMMARY.md
  modified:
    - contracts/scripts/deploy.js
    - contracts/hardhat.config.js
    - frontend/src/config/contracts.ts

key-decisions:
  - "deploy.js writes deployments/{network}.json via fs.writeFileSync after all deployments complete — canonical address source"
  - "nftContractV1 preserved when existing deployment has different NFTContract address — supports Dashboard dual-address enumeration"
  - "COLLECTION section in contracts.ts has no address field — clients pass clone addresses returned by CollectionFactory"
  - "hardhat.json committed to repo; Sepolia deployments should also be committed after live deploy"

patterns-established:
  - "All deploy scripts follow: deploy → record address → write JSON → print verification commands"
  - "contracts.ts uses import.meta.env.VITE_*_ADDRESS with zero-address fallback for new contracts"

# Metrics
duration: 4min
completed: 2026-02-21
---

# Phase 1 Plan 06: Deploy Infrastructure and Contract Config Summary

**Unified deploy.js for all 4 Phase 1 contracts with Etherscan verification support, deployments JSON registry, and complete ABI coverage in contracts.ts**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-21T08:49:21Z
- **Completed:** 2026-02-21T08:53:08Z
- **Tasks:** 2 (plus checkpoint)
- **Files modified:** 5

## Accomplishments

- deploy.js rewrote to deploy NFTContract, Marketplace, AuctionHouse, CollectionFactory in dependency order; retrieves collectionImplementation from factory
- Writes `deployments/{network}.json` with all 5 addresses; preserves prior `nftContract` as `nftContractV1` on redeploy
- Prints Etherscan `npx hardhat verify` commands and `VITE_*` env var values after Sepolia deployment
- `hardhat.config.js` gets `etherscan.apiKey` block reading from `ETHERSCAN_API_KEY` env var
- `contracts.ts` updated with complete human-readable ABI strings for: NFT (royalty-aware), Marketplace (offers + updated ItemSold 5-arg), AuctionHouse (English + Dutch), CollectionFactory, Collection (no fixed address — per-clone)
- All 175 tests pass; frontend builds cleanly (TypeScript + Vite)

## Task Commits

Each task was committed atomically:

1. **Task 1: Update deploy script, hardhat config, and create deployments registry** - `02f61db` (feat)
2. **Task 2: Update frontend contracts.ts with new ABIs and addresses** - `15ccabf` (feat)

**Plan metadata:** _(docs commit follows)_

## Files Created/Modified

- `contracts/scripts/deploy.js` - Rewrote to deploy all 4 contracts; writes deployments JSON; preserves V1 address; prints Etherscan verification commands and VITE_ env var values
- `contracts/hardhat.config.js` - Added `etherscan.apiKey` block
- `contracts/deployments/.gitkeep` - Created directory for deployment JSON files
- `contracts/deployments/hardhat.json` - Generated by test deploy run; all 5 addresses present
- `frontend/src/config/contracts.ts` - Added AUCTION_HOUSE, COLLECTION_FACTORY, COLLECTION sections; updated NFT (royaltyInfo, supportsInterface, 2-arg mintNFT); updated MARKETPLACE (offer functions, events, updated ItemSold with royalty arg)

## Decisions Made

- **COLLECTION has no address field:** Each EIP-1167 clone is deployed at a distinct address returned by `CollectionFactory.createCollection()`. Frontend passes that address when constructing ethers Contract instances.
- **deployments/hardhat.json committed:** Provides known Hardhat addresses as reference for local development. Sepolia JSON should be committed after live deployment.
- **Zero-address fallback for new contracts:** `AUCTION_HOUSE_ADDRESS` and `COLLECTION_FACTORY_ADDRESS` default to `0x0000...0000` when env vars missing. Frontend pages for those features should check `address !== zeroAddress` before calling.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

For Sepolia deployment (not yet performed):
- `ETHERSCAN_API_KEY` from Etherscan dashboard
- `ALCHEMY_SEPOLIA_URL` from Alchemy dashboard
- `DEPLOYER_PRIVATE_KEY` from MetaMask (export private key)

Set these in `contracts/.env` then run:
```bash
cd contracts && npx hardhat run scripts/deploy.js --network sepolia
```

The script prints verification commands and env var values on completion.

## Next Phase Readiness

- Phase 1 Contract Foundation is complete. All 4 contracts deployed locally and tested (175 tests passing).
- Sepolia deployment is deferred to user (requires env vars with real keys).
- Phase 2 subgraph can begin: deployment addresses from `deployments/sepolia.json` after live deploy.
- Phase 3 frontend has updated `contracts.ts` with complete ABIs for all contracts.
- Outstanding concern: Dashboard must handle both `NFTContract` (new, with royalties) and `nftContractV1` (old, without) when enumerating user NFTs — explicit decision needed in Phase 3.

---
*Phase: 01-contract-foundation*
*Completed: 2026-02-21*
